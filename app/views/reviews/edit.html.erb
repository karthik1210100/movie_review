<div class="container py-5" style="max-width: 700px;">

  <div class="card shadow-sm border-0 rounded-4 p-4 mb-4">
    <h2 class="mb-4 text-primary fw-bold text-center">✏️ Edit Review</h2>

    <%= render "form", review: @review %>
  </div>

  <div class="text-center">
    <%= link_to "← Back to Movie", movie_review_path(@movie), class: "btn btn-outline-secondary px-4 rounded-pill" %>
  </div>

</div>

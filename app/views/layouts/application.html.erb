<!DOCTYPE html>
<html>
<head>
  <title>Movie Review</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= favicon_link_tag asset_path("rate.png") %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>

  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">

  <!-- jQuery, Popper, and Bootstrap JS -->
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

  <style>
      body {
          background-color: #f4f6f9;
          font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
          margin: 0;
          padding: 0;
      }

      .navbar {
          box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }

      .navbar h5 {
          margin-left: 10px;
          margin-right: auto;
          font-size: 16px;
          font-weight: 600;
          color: #333;
      }

      .navbar .dropdown-toggle {
          margin-left: auto;
      }

      .dropdown-menu {
          background-color: #ffffff;
          border-radius: 6px;
          box-shadow: 0 8px 16px rgba(0, 0, 0, 0.15);
      }

      .dropdown-item {
          color: #333;
          transition: background-color 0.2s;
      }

      .dropdown-item:hover {
          background-color: #f0f0f0;
      }

      .alert, .notice {
          margin: 15px auto;
          max-width: 600px;
          padding: 12px 20px;
          border-radius: 5px;
          font-size: 14px;
      }

      .alert {
          background-color: #f8d7da;
          color: #721c24;
      }

      .notice {
          background-color: #d4edda;
          color: #155724;
      }

      .container-fluid {
          display: flex;
          align-items: center;
          justify-content: space-between;
          padding: 10px 20px;
      }

      img.round-avatar {
          border-radius: 50%;
          height: 40px;
          width: 40px;
          object-fit: cover;
      }
  </style>
</head>

<body>
<% if user_signed_in? %>
  <nav class="navbar navbar-expand-lg" style="background-color: #CCCCCC;">
    <div class="container-fluid">
      <% if current_user.avatar.attached? %>
        <%= image_tag(current_user.avatar, size: '40x40', class: 'round-avatar') %>
      <% else %>
        <img src="https://ui-avatars.com/api/?name=<%= current_user.email %>" alt="avatar" class="round-avatar">
      <% end %>
      <h5><%= current_user.email %></h5>

      <div class="dropdown">
        <a class="btn btn-secondary dropdown-toggle" href="#" id="userDropdown" data-toggle="dropdown">
          User
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
          <%= link_to 'Edit Profile', edit_user_registration_path, class: 'dropdown-item' %>
          <%= link_to 'Log Out', destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?' }, class: 'dropdown-item' %>
        </div>
      </div>
    </div>
  </nav>
<% else %>
  <nav class="navbar navbar-expand-lg justify-content-between" style="background-color: #CCCCCC;">
    <div class="container-fluid">
      <h3 class="text-dark m-0">Movie Review</h3>

      <div class="dropdown">
        <a class="btn btn-secondary dropdown-toggle" href="#" id="guestDropdown" data-toggle="dropdown">
          User
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="guestDropdown">
          <%= link_to 'Sign In', new_user_session_path, class: 'dropdown-item' %>
          <%= link_to 'Sign Up', new_user_registration_path, class: 'dropdown-item' %>
        </div>
      </div>
    </div>
  </nav>
<% end %>

<% if notice %><p class="notice"><%= notice %></p><% end %>
<% if alert %><p class="alert"><%= alert %></p><% end %>

<%= yield %>
</body>
</html>

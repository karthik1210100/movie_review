<%= form_with(model: [@movie, @review, user_review_comment]) do |form| %>
  <% if user_review_comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_review_comment.errors.count, "error") %> prohibited this user_review_comment from being saved:</h2>

      <ul>
        <% user_review_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :comment, style: "display: block" %>
    <%= form.text_field :comment, required: true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
